---
import type { CollectionEntry } from "astro:content";
import BackArrowIcon from "../components/BackArrowIcon.astro";
import FormattedDate from "../components/FormattedDate.astro";
import Root from "./Root.astro";

type Props = CollectionEntry<"blog">["data"];

const { title, description, pubDate, updatedDate, heroImage } = Astro.props;
---

<Root title={title} description={description} image={heroImage}>
  <a
    class="inline-flex items-center gap-2 py-1.5 -mt-1 mb-6 font-[JetBrains_Mono] text-base text-[var(--fg-muted)] transition-[color,transform] duration-200 ease-out hover:text-[var(--accent-orange)] hover:-translate-x-px"
    href="/blog"
  >
    <BackArrowIcon /><span class="underline decoration-dotted underline-offset-4">Back to posts</span>
  </a>
  <article>
    <div class="hero-image !mb-5">
      {
        heroImage && (
          <img
            class="w-full h-[260px] object-cover rounded-none border border-[var(--line)]"
            src={heroImage}
            alt=""
          />
        )
      }
    </div>
    <div class="prose">
      <div class="title !mb-8 pb-6 border-b-2 border-[var(--line)]">
        <h1 class="font-[JetBrains_Mono] font-semibold">{title}</h1>
        <div class="date mt-3 text-base text-[var(--fg-muted)] tabular-nums">
          <FormattedDate date={pubDate} />
          {
            updatedDate && (
              <div class="last-updated-on mt-1">
                Last updated on <FormattedDate date={updatedDate} />
              </div>
            )
          }
        </div>
      </div>
      <slot />
    </div>
  </article>
</Root>
